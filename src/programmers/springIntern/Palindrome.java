package programmers.springIntern;

/**
 * 팰린드롬 : 앞에서 읽거나 뒤에서 읽거나 같은 배열
 * 순서
 * 1). 배열에서 0이 아닌수 선택
 * 2). 고른 수 - 1
 * 3). 1), 2), 를 통해 팰린드롬 완성 -> 승리
 * 4). 팰린드롬 X -> 상대방에게 턴이 넘어감
 * */
public class Palindrome {
    public int[] solution(int[][] queries) {
        int[] answer = {};
        return answer;
    }
}
